
// Cursor Actions

ACTION(toggleCursorVisibility, WindowManager::get().toggleCursorVisibility())
ACTION(toggleCursorCapture, WindowManager::get().toggleCursorCapture())

// Window Actions

ACTION(toggleBorderlessFullscreen, WindowManager::get().toggleBorderlessFullscreen())

// Screenshot Actions

ACTION(takeHudlessScreenshot, RSManager::get().enableTakeScreenshot())

// Graphics Actions

ACTION(toggleAA, RSManager::get().toggleAA())
ACTION(toggleVSSAO, RSManager::get().toggleVssao())
ACTION(toggleDofGauss, RSManager::get().toggleDofGauss())

ACTION(toggleHUD, RSManager::get().toggleHideHud())
ACTION(toggleHUDChanges, RSManager::get().toggleChangeHud())

ACTION(toggle30FPSLimit, Settings::get().toggle30FPSLimit())

// Development Actions

ACTION(reloadVSSAOEffect, RSManager::get().reloadVssao())
ACTION(reloadHBAOEffect, RSManager::get().reloadHbao())
ACTION(reloadSCAOEffect, RSManager::get().reloadScao())
ACTION(reloadGAUSSEffect, RSManager::get().reloadGauss())
ACTION(reloadAAEffect, RSManager::get().reloadAA())

ACTION(singleFrameFullCapture, RSManager::get().enableSingleFrameCapture())
ACTION(userTrigger, SDLOG(0, "================================================================= USER TRIGGER ===\n"))
ACTION(reloadHUDVertices, RSManager::get().reloadHudVertices());

ACTION(manualBackup1, SaveManager::get().manualBackup(1));
ACTION(manualRestore1, SaveManager::get().manualRestore(1));

ACTION(manualBackup2, SaveManager::get().manualBackup(2));
ACTION(manualRestore2, SaveManager::get().manualRestore(2));

ACTION(manualBackup3, SaveManager::get().manualBackup(3));
ACTION(manualRestore3, SaveManager::get().manualRestore(3));

ACTION(manualBackup4, SaveManager::get().manualBackup(4));
ACTION(manualRestore4, SaveManager::get().manualRestore(4));

ACTION(manualBackup5, SaveManager::get().manualBackup(5));
ACTION(manualRestore5, SaveManager::get().manualRestore(5));
